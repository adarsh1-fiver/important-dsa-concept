for (String op : operations)
for (int i = 0; i < operations.length; i++) {
    String op = operations[i];
}

Integer.parseInt(op) = convert string → integer so it can be used in math and stored in stack

//it also conv 
Integer.valueOf(op) does
Converts a String → Integer object

//sum of digit
int sum = 0;

        while (num != 0) {
            sum = sum + (num % 10); // Extract last digit
            num = num / 10;         // Remove last digit
        }

//square of digit and sum

        int sum = 0;

        while (num != 0) {
            int digit = num % 10;
            sum += digit * digit;
            num /= 10;
//linked list
class ListNode {
    int val;
    ListNode next;

    ListNode(int val) {
        this.val = val;
    }
}

Most commonly used in interviews:

ListNode(int val)
ListNode(int val, ListNode next)


//Comparator.comparingInt(...)

Creates a comparator that compares integers.

public int compare(int[] a, int[] b) {
This method compares two items a and b.
Java's sorting methods call this repeatedly to determine ordering.

Comparator.comparingInt(a -> a[1])

means:

Compare rows based on their second column values.


Double.compare(a1, b1)

would sort in ascending order.


//int[][] items = new int[n][2];
Line 3

Creates a 2D array.

n rows → one row per item.
2 columns:
Column 0 → value
Column 1 → weight





//items[i][0] = val[i]; 
//items[0][1] = wt[0]
            = 10

//2d array
 int n = matrix.length;
        int m = matrix[0].length;
