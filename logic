for (String op : operations)
for (int i = 0; i < operations.length; i++) {
    String op = operations[i];
}

Integer.parseInt(op) = convert string → integer so it can be used in math and stored in stack

//it also conv 
Integer.valueOf(op) does
Converts a String → Integer object
